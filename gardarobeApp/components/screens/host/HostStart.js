// Import necessary components from React Native
import { View, Text } from "react-native";
import React from "react";

// Define the HostStart functional component
const HostStart = () => {
  // Render the component
  return (
    // Use a View component as a container
    <View>
      {/* Display the text instruction inside a Text component */}
      <Text>Scan QR koden. Scan derefter bøjlen</Text>
    </View>
  );
};

// Export the HostStart component for use in other parts of the app
export default HostStart;
